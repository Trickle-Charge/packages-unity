name: Publish Third-Party Packages

on:
  push:
    paths:
      - 'Packages/**'

jobs:
  trigger-shared-pipeline:
    uses: Trickle-Charge/TrickleCharge-Infra/.github/workflows/shared-registry-publish.yml@main

    secrets: inherit